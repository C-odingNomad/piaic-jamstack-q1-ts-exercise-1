// Date: 24/12/2023
// Zeeshan Hassan
// No Users:- Checking the Empty List (array)
var users = ["iman", "john", "michael", "admin", "alex"];
users.splice(0, 5);
// console.log(users)
if (users.length === 0) {
    console.log("We need to find some users!");
}
